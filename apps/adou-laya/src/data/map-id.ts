// MapId — the four battle maps.
//
// Faithful reconstruction of the original bundle's `x` enum
// (reconstruction/reference/bundle.pretty.js lines ~1138-1145): 巨鹿 / 云梦泽 /
// 虎牢关 / 赤壁.

export enum MapId {
  JuLu = 0,
  YunMengZe = 1,
  HuLaoGuan = 2,
  ChiBi = 3,
}
