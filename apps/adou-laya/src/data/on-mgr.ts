// OnMgr — game hub sub-manager `_` (F.On).
//
// Faithful reconstruction of the original bundle's `_` class
// (reconstruction/reference/bundle.pretty.js lines ~1132-1136). It is trivial in
// the bundle (a single field `xh = 6` and an empty init); its exact role is not
// evident from the source, so the field is kept verbatim and annotated.

export class OnMgr {
  /** Purpose not evident in the bundle; kept verbatim. */
  readonly xh = 6;

  init(): void {}
}
