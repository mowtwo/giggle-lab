// Ambient runtime globals provided by the host page / platform SDKs (or by the
// static bootstrap js/adou-local-bootstrap.js). Declared so reconstructed code
// can feature-detect them exactly as the original bundle did.

/* eslint-disable @typescript-eslint/no-explicit-any */

declare const h5api: any;
declare const gamebox: any;
